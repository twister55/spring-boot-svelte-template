// SPA mode: the app is served by Spring Boot as static files (adapter-static
// with fallback: index.html), so there is no server-side rendering.
export const ssr = false;
export const prerender = false;
